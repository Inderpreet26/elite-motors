apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elite-motors.fullname" . }}-test-connection"
  labels:
    {{- include "elite-motors.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elite-motors.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
