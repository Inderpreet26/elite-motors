apiVersion: v1
kind: Service
metadata:
  name: {{ include "elite-motors.fullname" . }}
  labels:
    {{- include "elite-motors.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elite-motors.selectorLabels" . | nindent 4 }}
